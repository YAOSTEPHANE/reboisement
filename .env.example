# URL publique du site (pour les redirections Stripe)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Stripe — paiement en ligne (optionnel)
# Sans STRIPE_SECRET_KEY, seul le formulaire « Être recontacté » est disponible.
STRIPE_SECRET_KEY=sk_test_...
# Webhook : créez un endpoint dans le tableau de bord Stripe pointant vers https://votredomaine.com/api/stripe/webhook
STRIPE_WEBHOOK_SECRET=whsec_...
# Clé publique non utilisée côté serveur pour ce flux Checkout ; à configurer si vous utilisez Stripe Elements.
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...
